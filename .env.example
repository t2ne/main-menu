# This value is embedded into client JS (PUBLIC_*), so it isn't secret.
# Use a tailnet-only HTTPS URL (e.g. a `tailscale serve` endpoint on an always-on node).
PUBLIC_TAILNET_PROBE_URL=https://your-device.your-tailnet.ts.net/health
# Use a tailnet-only host (scheme optional). You can provide two hosts:
# `desktopHost,mobileHost` (mobile optional).
